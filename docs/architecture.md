# Public architecture

Vidal Intelligence presents a small, reusable pipeline: inspect a video, detect objects, maintain tracks, convert observations into an Event Schema, optionally create visual embeddings, search by text, and record evidence in an Experiment Card.

The public layer is intentionally a demonstration, not the complete private laboratory.
